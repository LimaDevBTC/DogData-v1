# Progresso das frentes DogData da troca (FS1, FS2, FS3)

Plano: `dogcity-mundo/LIGAR.md` (secao 2 e FS1 a FS3 da secao 5). Worktree `DogData-v1-cidade-nova`,
branch `cidade-nova`, criada de `main` em `50524444db`. Nada aqui foi empurrado nem mesclado: merge,
push, env da Vercel e migracao sao do fundador.

- [FS] worktree criada fora da arvore principal (o bot de hora em hora so faz `add -A` e `push origin main` na arvore principal; esta branch nunca e empurrada por ele). `node_modules` e um link para o da arvore principal, so para rodar `tsc` e `next dev` aqui; nao entra em commit
- [FS1] `next.config.js`: rewrite `beforeFiles` da /city para `DOGCITY_MUNDO_ORIGEM` (so aceita `https?://host`, sem barra no fim). Tres regras: `/city` (menos `?classic=1` e `?view=war`) para `<origem>/city/index.html`; `/city/:dir(assets|dados|modelos|texturas|draco|basis)/:path*`; `/city/:arq(manifesto.json|creditos.json)`. Sem env valida a chave `rewrites` nem existe e `headers()` devolve a lista de antes. Com env, `headers()` ganha `x-vercel-enable-rewrite-caching` (1 nos seis diretorios, 0 no HTML e nos dois json)
- [FS1] DESVIO do LIGAR.md 2.2, de proposito: `?view=war` fica na praca antiga POR PADRAO e so vai para o jogo com `DOGCITY_GUERRA_ANTIGA=0` (no plano era o contrario: so ficava na antiga com `=1`). Motivo: o jogo ainda nao tem batalha e a porta 02 da landing e o portao da WebView mandam para `?view=war`; esquecer uma env em Production nao pode mandar esse publico para uma cidade sem batalha. Quem testar a batalha nova no preview (T1) poe `DOGCITY_GUERRA_ANTIGA=0` no Preview
- [FS1] prova da tabela de rotas (sem `next build`): `loadCustomRoutes` + `buildCustomRoute` do proprio Next 15.4.10 sobre o `next.config.js` de HEAD e o novo. Sem env e com env invalida (`https://www.dogdata.xyz/`, barra no fim): JSON identico ao de HEAD, sha256 `086ad13e545b...` nos tres. Com env: 3 rewrites em beforeFiles, afterFiles e fallback vazios
- [FS1] colisao: as regex geradas contra os 334 arquivos de `public/city` (git e disco), as 7 rotas de `app/city` e os caminhos da landing e do lookup (346 caminhos): zero casam. Os caminhos do jogo (11, inclusive `/city/`) casam todos; `/city/manifestoXjson` nao (ponto escapado)
- [FS1] consultas (matchHas do Next): `/city`, `?view=park`, `?view=home`, `?addr=`, `?s=1`, `?view=warzone` vao ao jogo; `?classic=1`, `?view=war` e os dois juntos ficam na praca antiga. Com `DOGCITY_GUERRA_ANTIGA=0`, so `?classic=1` fica
- [FS1] fumaca com `next dev -p 3107` nesta worktree (sem `.env`, entao sem Supabase) e uma origem falsa em :5199: `/city`, `/city?view=park`, `/city/assets/*.js`, `/city/dados/*`, `/city/manifesto.json` vieram da origem falsa; `/city/carta.svg`, `escrituras.bin`, `hero-mapa.jpg` com o mesmo sha256 do disco; `mapa-topo.svg` e `kray-tower.glb` 200; `/city?classic=1` e `/city?view=war` abriram a praca antiga ("Satoshi Plaza · DogCity on the Moon") com ZERO pedido na origem do jogo; `/city/war` 200. Os dois servidores de teste foram parados pelo PID da porta
- [FS1] limite conhecido: no `next dev` os headers do `next.config` nao saem na resposta do rewrite externo (a Vercel aplica na borda). O `x-vercel-enable-rewrite-caching` so se confere no preview (`x-vercel-cache: HIT` na segunda chamada de `/city/assets/*`); se nao honrar, mover as tres regras de header para o `vercel.json`
- [FS2] `lib/api/prazo.ts` (novo): `comPrazo(fn(signal), {ms, falha})` corre a rota contra o prazo e aborta o que estava em voo (`.abortSignal()` do supabase-js); `falhaPublica()` = 503 com `s-maxage=10`, `falhaPrivada()` = 503 `no-store`. Mesma ideia do disjuntor da arvore (26/08), com prazo e resposta de quem chama
- [FS2] `GET /api/chat`: `Cache-Control: public, s-maxage=2`, prazo de 5 s, falha vira 503 cacheado 10 s (antes: 500 sem cache, todo poll redetonava). Cada mensagem ganha `avatar_inscription_id` por UMA consulta extra em `dogcity_profiles` pela chave primaria (`in` nos ate 50 enderecos); se ela falhar o chat sai sem foto, nunca sem mensagem. `POST` devolve o `avatar_inscription_id` do autor junto (le no mesmo select do handle)
- [FS2] consequencia do cache de 2 s na praca antiga: `components/wallet/city-chat.tsx` (fora do dono listado no plano, mas e a unica forma de a FS2 continuar compativel com a /city antiga) agora poe na lista a mensagem que o POST devolveu, sem refazer o GET, e o GET seguinte preserva as locais com id maior que o ultimo que veio. Sem isso a mensagem enviada piscava (aparecia, sumia no GET cacheado, voltava). Regra para o F4: quem posta usa a resposta do POST
- [FS2] `GET /api/war/trades`: `s-maxage=30, stale-while-revalidate=30` (o cache em memoria so valia por instancia); prazo de 6 s na Kraken (`AbortSignal.timeout`); resposta vazia (Kraken fora) fica 5 s na borda e NAO entra no cache em memoria
- [FS2] `GET /api/profile/batch?addresses=a,b` (nova): ate 50 enderecos validos distintos (mais que isso = 400 com `max`, nunca corte calado), handle e `avatar_inscription_id`, UMA consulta por chave primaria em minusculas (como o POST grava), sem ler cookie, `s-maxage=60, stale-while-revalidate=300`; endereco sem perfil nao volta no mapa (convencao de /api/identity); chave do mapa = endereco como veio. Prazo 5 s
- [FS2] `lib/wallet/message.ts`: linha `URI: https://www.dogdata.xyz` no desafio, entre o aviso e `Address:` (constante `CHALLENGE_URI`). O verify compara com a string guardada no Redis, entao desafio ja emitido segue valendo. Texto em ingles (e tela: aparece no popup da carteira)
- [FS2] migracao do funil ESCRITA, NAO APLICADA: `supabase/migrations/032_funil_cidade.sql` (o plano dizia 028, mas 028 ja foi consumido pela 027 "027+028" e o repo esta em 031). So troca `analytics_funnel_etapas`: 7 etapas, "Entered the city" (pageview `/city` ou `/city/%`) e "Found their lot" (`find_my_lot` com `event_meta.found = true`) entre "Saw the offer" e "Copied the address". Corpo de partida conferido contra o banco com `pg_get_functiondef` (so leitura); as duas consultas novas rodadas no banco numa janela de 1 dia, so leitura: 11 sessoes na cidade, 0 lotes
- [FS2] `app/admin/analytics/conversao.tsx`: a nota "as etapas do meio estao em zero" achava as etapas por indice (2 a 4); agora acha pelo nome, e o painel le certo com 5 ou 7 etapas (antes e depois de aplicar a 032)
- [FS2] `npx tsc --noEmit` verde (base tambem verde). Sem lint: o repo nao tem config de ESLint e `next lint` pediria para criar uma
- [FS3] `app/api/profile/route.ts`: o lote sai do registro selado, nao mais de `dogcity_lots` (registro da CrossChainCity). Campo novo `city` (o `lot` antigo saiu; o unico consumidor era a propria /profile): `{block, status: lot|headstone|not_in_snapshot|unavailable, dog, position, lot {lot_id, sector, district, typology, dsc, area_m2, map}, headstone {id, place, map}, lot_floor_dog}`. MESMA regra de `/api/dogcity/lookup`: `dog_snapshot_lookup` (uma consulta pela chave primaria, no lugar da de `dogcity_lots`, entao continuam 3 consultas) decide lote ou lapide pelo `destino`; `buscarEscritura` da posicao e area gravada, com teto de 1,5 s (sem indice a tempo: `position: unavailable`, nunca lote inventado). Area da tabela nao entra. Endereco da cidade normalizado como no lookup (bech32 minusculo, base58 intacto; o perfil segue em minusculas). Rota inteira sob `comPrazo` de 5 s com 503 `no-store` (le cookie, nao cacheia)
- [FS3] `next.config.js`: `outputFileTracingIncludes` de `/api/profile` leva `public/city/escrituras.bin` (sem isso a funcao baixava os 3 MiB pela URL a cada instancia fria). A tabela de rotas sem env continua identica a de HEAD (conferido de novo)
- [FS3] `app/profile/page.tsx`: cartao novo `CityDeedCard`. Lote: "Lot S04-Q12-B003-L006", Area (registro), Curve target (`alvoDaCurva`, a mesma funcao da landing; a area prometida nao e servida), Sector (com o nome publicado so nos setores 7, 8, 9), Typology, nota curta quando a area fica abaixo do alvo, "Registered at block 966,670", Open in DogCity (`/city?addr=<endereco>`) e On the map (`/city/mapa?lot=`). Lapide: "Headstone L09444", saldo no bloco contra o corte publicado 591.95 e o piso de 24 m2, e as frases da landing (contrato publico) sobre o direito de mintar depois. Fora do snapshot: "Arrived after the founding" com o texto do anel 2 da landing. Registro fora do ar: texto proprio, sem numero
- [FS3] copy corrigida: o subtitulo (antigo `:864`, "Every wallet with DOG gets a plot on the lunar map") virou "Every wallet that held DOG at block 966,670 is in the sealed city registry: a lot if its balance reached the smallest lot the city builds, a headstone if it did not. Donors get a plaque in the Founders Register."; o texto de sem lote (antigo `:898`, "hold DOG and the map builds your address in", promessa falsa) saiu, no lugar entrou o cartao de fora do snapshot. "Tier" do plano lido como a tipologia do registro (a unica escada que o registro tem por lote); a licenca de doacao continua no cartao vizinho
- [FS3] conferencia com `next dev -p 3107` nesta worktree, env lida por link temporario para o `.env.local` da arvore principal (apagado depois; so GET): `/api/profile` contra `/api/dogcity/lookup` em 5 enderecos (lote bc1p, lote base58 `3FX8...`, lapide, lote com handle, fora do snapshot): status, dog, lot_id, setor, tipologia, area e lapide iguais nos 5. `/api/chat`: 22 mensagens, 5 com foto, header `public, s-maxage=2`. `/api/war/trades`: 200 trades, header certo. `/api/profile/batch`: 3 perfis de 4 enderecos validos + 1 invalido (o sem perfil e o invalido nao voltam), 400 sem lista e com 51, cookie nao muda a resposta
- [FS3] chapas headless (chrome-headless-shell, sem janela) do cartao em desktop 1280 e celular 390 nos 3 casos: `dogcity-mundo/chapas/ligar/FS3/profile_{lote,lapide,fora}_{desktop,celular}.png` (nao commitadas la). Todo pedido que nao era GET foi abortado no navegador (6 `POST /api/ads/track`) e o analytics ficou desligado por opt-out: nada escrito em producao. No celular o id do lote partia no meio de um numero com `break-all`; trocado por `break-words` e `text-lg`
- [FS] `npx tsc --noEmit` verde no fim. Sem `next build` (instrucao: derruba o dev); o build verde com e sem env continua sendo o passo `[F]` antes do merge
